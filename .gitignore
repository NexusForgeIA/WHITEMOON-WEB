# Claude Code local files
.claude/settings.local.json
.claude/launch.json
.git-commit-msg-tmp.txt
.pr-body-tmp.md
.scripts-tmp/

# Borradores y mockups que genera Claude en local. Estan en la raiz, asi que
# GitHub Pages los serviria (path: '.' + .nojekyll): no deben entrar al repo.
# Anclado con "/" a proposito: solo la carpeta de la raiz, no cualquier
# directorio que se llame igual mas abajo.
/Claude outputs/

# SEO Guardian generated report (CI artifact)
seo-guardian-report.md

# Bytecode de Python (aparece al importar seo_guardian.py en local)
__pycache__/

# Blog generator run summary (transient; leido por blog_notify.py en el mismo job)
scripts/last-run.json
